<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="#">
        <img alt="Password" src="...">
      </a>
      {{#link-to 'plist'}}
        <button type="button" class="btn btn-default navbar-btn">Passwords</button>
      {{/link-to}}
    </div>
  </div>
</nav>

<h1>Password Home Page (index)</h1>

{{outlet}}